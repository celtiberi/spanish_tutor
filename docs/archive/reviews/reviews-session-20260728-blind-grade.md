

---

## GROK (Grok Build CLI (94172f2aa4e5)) - 2026-07-28 17:00 UTC
*Headless via grok_collab.py; genuine output appended verbatim; sandboxed cwd (inlined files only).*

I'll extract the full learner–tutor dialogue from the inlined session so I can grade against the rubric without the truncated middle.I have the full 12-turn dialogue. Scoring strictly from the transcript and rubric next.# Blind evaluation — session `20260728-103617-conversational-web`
**Evaluator:** Grok (independent, blind)  
**Date of evaluation:** 2026-07-28  
**Data used:** frozen rubric `blind-rubric-20260728.md` + session transcript only (12 learner–tutor turns; state/JSON treated as non-scoring metadata except image captions/concepts attached to turns).  
**Scope:** grade the **tutor**, adult absolute-beginner (A1) Spanish in chat.

**Quantitative summary**  
Dimension scores (1–10): 7, 6, 8, 7, 7, 6, 5, 8  
Sum = \(7+6+8+7+7+6+5+8 = 54\)  
Mean = \(54 / 8 = 6.75\)  
Letter mapping used: \(9.0\text{–}10=\mathrm{A}\), \(8.0\text{–}8.9=\mathrm{B+}\), \(7.0\text{–}7.9=\mathrm{B}\), \(6.5\text{–}6.9=\mathrm{B-}\), \(6.0\text{–}6.4=\mathrm{C+}\), \(5.0\text{–}5.9=\mathrm{C}\), \(4.0\text{–}4.9=\mathrm{D}\), \(<4=\mathrm{F}\).  
**Overall from mean: B-** (momentum alone would pull toward C+; explanations/warmth hold it at B-).

---

## Dimension scores

### 1. Comprehensibility — **7/10**
Most tutor Spanish is short and often glossed, so an A1 adult can usually follow (e.g. Turn 1: «**me llamo** (my name is) Marisol» + sí/no check; Turn 8: «¿Tienes un café en la sala?»). Plausible comprehension failures: Turn 2’s try «Si digo «¡Hola!», ¿qué dices tú?» triggers an explicit breakdown request in Turn 3 («digo and dices.. can I get a breakdown»), and Turn 11’s «En la imagen vemos **el café**» produces Turn 12 «"vemos" - what is this?» — both show Spanish that did not land unaided.

### 2. New-vocabulary support — **6/10**
Support is **inconsistent**. Strong cases: Turn 1 gloss on *me llamo*; Turn 3 «**Digo** = yo… **Dices** = tú»; Turn 6 «*Afuera* significa *outside*»; Turn 9 «**Hago el desayuno** (breakfast = *el desayuno*)»; Turn 10 «**su bote**»; Turn 12 «**Vemos** means "we see"». Unsupported or under-supported arrivals: Turn 5 «¡Mucho gusto, Patrick!» with no gloss; Turn 5–6 *trabajo* / *hermano* without help; Turn 6 weather recast «no llueve» never ties learner’s «uvia (rain)» to a clear target form; Turn 11 *por la mañana* and *bebe* arrive mainly by context; Turn 12 piles on «¿Comes **pan** (bread)…» while still repairing *vemos*/*bebo*.

### 3. Explanation quality — **8/10**
When the learner asks direct language questions, answers are usually correct, short, and actionable.  
- **Best:** Turn 10 — learner «en *his* bote» → «Para decir «his boat», decimos: **su bote**. Así: «Mi amigo Paul **está en su bote**»» (form + full model sentence). Near-tie: Turn 3 *digo*/*dices* person split.  
- **Worst:** Turn 2 — after «tu llama es Marisol», «Decimos: **Te llamas** Marisol (o **Me llamo** Marisol)» equates 2sg and 1sg as if interchangeable fixes; that is person-muddled for a beginner still producing *llama es*.  
Turn 12 correctly handles both *vemos* and *yo bebo*, though it bundles two teaching points in one reply.

### 4. Responsiveness — **7/10**
Explicit language questions are generally answered in-turn: Turn 3 (digo/dices), Turn 7 («To ask "Where am I?", say **¿Dónde estoy?**»), Turn 10 (*his* → *su*), Turn 12 (*vemos* + conjugation). Gaps: Turn 6 learner offers weather («No uvia…») and the tutor pivots to «¿…amigo o hermano?» without finishing the weather form; Turn 9 learner is cooking breakfast («Yo hacer… deysayunas. Papas y savoyes») and after a good *hago* note the try jumps to «¿Y tu amigo Paul?». Turn 12 orders **conjugation praise/fix before** answering the fronted «"vemos" - what is this?» question (both get answered, but not question-first).

### 5. Correction quality — **7/10**
Useful, non-punishing recasts appear for high-value forms: Turn 9 *Yo hacer* → **Hago el desayuno**; Turn 10 full *está en su bote*; Turn 12 *yo bebe* → **Yo bebo**. Turn 5 learner already produces clean «Me llamo Patrick» and is not re-drilled on the old *me llamo es* pattern — good restraint. Weaknesses: Turn 2 opens with «¡Sí, exacto!» on an incorrect form («tu llama es…») before repairing; Turn 6 never clearly models *No llueve hoy* against «uvia»; Turn 7 leaves «circa» uncorrected (*cerca*); accent/agreement on *está* is rarely made salient.

### 6. Media appropriateness — **6/10**
**Thin evidence on pixels:** only concept/form/caption metadata is in the log, not the image files.  
- Turn 3: image concept `hola` / caption «greeting — hello» while the talk is *digo*/*dices* conjugation — weak match to the actual teaching point.  
- Turns 11–12: `cafe` / «coffee» aligns with «Paul bebe café» / morning-coffee try — appropriate.  
- Turns 1–2, 4–10: no images; no spurious media there.  
No evidence of a boat/kitchen image when those topics were live (Turns 7–10), so media does not systematically track content.

### 7. Momentum and progression — **5/10**
The session **moves**, but not along a clear A1 skill ladder the learner could name at the end. Arc: name check → *hola* production → meta *decir* → wellbeing → location → weather (dropped) → friend location → room → breakfast (partially dropped) → boat → coffee → bread. Difficulty roughly increases (longer phrases, 3sg *bebe*, possessives), and some links exist (friend → boat → drinks coffee), yet there is no stable communicative goal, no spiral return to a can-do, and no end-of-session sense of «you can now X». Closing/leave-taking language never appears despite a long chat.

### 8. Register and warmth — **8/10**
Tone fits an adult: brief praise («¡Qué bien!», «¡Ah, perfecto!», «¡Mmm, qué rico!») without baby-talk or heavy sugar. English rescue is used for metalanguage without talking down. No evidence of scolding. Slight risk of over-cheer («¡Sí, exacto!» on errors) but still adult-appropriate.

---

## Overall grade: **B-**

Mean dimension score \(6.75/10\) maps to **B-**. The tutor is a **competent micro-explainer**: when the learner asks “what is X / how do I say Y,” answers are usually right-sized and usable (Turns 3, 7, 9–10, 12), and warmth stays adult. The same session is a **weak curriculum**: new words often arrive bare (notably *Mucho gusto*), content the learner initiates is dropped (weather Turn 6, breakfast Turn 9), one early repair confuses person (*Te llamas* / *Me llamo*, Turn 2), and the chat hopscotches without a finish line. A beginner would likely leave liking the tutor more than knowing what they can now do in Spanish.

---

## Top 5 concrete defects (by learning impact)

1. **Topic abandonment after learner-initiated content (Turns 6, 9)** — Weather and breakfast production are discarded for friend/boat agenda.  
   **Fix:** One-turn uptake + target form (*No llueve hoy* / *Hago el desayuno con papas y cebollas*), then a try on **that** meaning before any pivot.

2. **Unsupported high-utility phrase: *Mucho gusto* (Turn 5)** — Social formula dropped with zero gloss/model contrast.  
   **Fix:** «**Mucho gusto** (nice to meet you)» once, then optional repeat try — do not introduce bare.

3. **Validating then muddling the name-form error (Turn 2)** — «¡Sí, exacto!» + «**Te llamas** … (o **Me llamo** …)» on «tu llama es Marisol».  
   **Fix:** Soften without “exacto”; one person only: «Decimos: **Te llamas** Marisol» (tutor about self: «**Me llamo** Marisol»).

4. **Incomplete form repair on learner-flagged items (Turns 6–7)** — «uvia» and «circa» never get clear targets (*llueve* / *cerca*).  
   **Fix:** Recast + one model sentence each; optional yes/no comprehension; do not move topic until form is heard once.

5. **No session arc / no closing skill (whole session; ends Turn 12 on *pan*)** — Difficulty wanders; learner cannot inventory gains.  
   **Fix:** Pick one interpersonal goal (e.g. short location + drink exchange **or** polite close), spiral it, end with a 1-line English “you practiced X.”

---

## Top 3 things done well

1. **Right-sized direct explanations** — Turn 3 (*digo*/*dices*), Turn 10 (*su bote* + full sentence), Turn 12 (*vemos* + *yo bebo*) are clear, correct, and immediately usable.  
2. **Scaffolded choice tries after models** — e.g. Turn 5 «¿…en casa o en el trabajo?», Turn 7 «¿…cocina o …sala?», Turn 10 «¿café o agua?» reduce production load for A1.  
3. **Adult warmth without punishment** — Affirmations stay brief; successful learner forms (Turn 5 «Me llamo Patrick», Turn 11 «Paul bebe cafe») are accepted rather than over-corrected.

---

## Thin-evidence refusals
- **Actual image content** (composition, language on image, mismatch beyond caption): not inspectable; media score rests on log `concept`/`caption` + tutor text only.  
- **Prior sessions / true “never seen” vocabulary for this learner:** unknown; “new” judged from this transcript’s first appearances and learner clarification requests only.  
- **Audio/pronunciation quality:** text chat only; not scored.  
- **Product sheet goals (IP-05, form weaning counts):** present in state JSON but **not used as the grading standard** (rubric: best practice for adult A1, no product context).

---

## Adjudication of the blind grade (⬛ Claude, 2026-07-28) — defects mapped to generators

Grade B- accepted as fair. Per PEDAGOGY §5 discipline (queued law: fix generators, not instances), each defect maps to its class:

1. **Topic abandonment (turns 6, 9 — weather, breakfast dropped for the boat/friend agenda).** Generator: the uptake standing order covers learner QUESTIONS but not learner-INITIATED CONTENT; mode targets (next_best/scene) pull the pivot. Fix class: one-turn content-uptake rule — model the learner's offered meaning (No llueve / Hago el desayuno) with a try on THAT meaning before any agenda pivot. Candidate law after countersign.
2. **"Mucho gusto" arrived bare (turn 5) — with the unscaffolded gate LIVE.** Generator hypothesis to verify: the gate scans model/try parts only; the phrase likely rode the acknowledge part. If confirmed: extend scan to all visible teaching parts. (It is in the association table, courtesy theme — the machinery knew it, the scan never looked.)
3. **"¡Sí, exacto!" on an incorrect form + two-person muddle (turn 2).** Generator: nothing forbids confirming praise on erroneous input, and repairs may offer 1sg and 2sg in one breath. Fix class: executor correction rules — never "exacto" on a form being repaired; ONE person per repair (CF-PROMPT DEBT territory).
4. **Learner-flagged errors never repaired (uvia, circa).** Generator: recast machinery is catalog-driven; self-noticed one-off lexical errors get no path. Fix class: learner-flagged-form uptake (they TOLD us they're unsure — highest-value correction moment).
5. **No session arc / no close.** Generator: r6's original architecture had a Close/enqueue phase; the thin phase controller dropped it. The task phase also needs verification against this session's log (did a scene bind?). Fix class: restore a 1-turn close move ("you practiced X" + a farewell exchange — which would also finally exercise the farewells the introduce router keeps planning).

Strengths (right-sized explanations, scaffolded A/B tries, adult warmth) are recorded as protected behavior — fix batches must not regress them. Fix batch queued behind in-flight agents.

---

## Defect batch landed (agent, 2026-07-28)

Adjudicated fix batch implemented; all four items landed. `pytest tests/ -q`: **470 passed, 17 subtests passed** (0 failures). `scripts/check_teacher_truncation.py`: ok (14 files). Server restarted; `/api/health` version `20260728-114151`, `stale_code: false`.

**1. Close phase (defect #5) — LANDED.** `tutor/session_phases.py`: `"close"` added to ACTIVITY_TYPES; `_with_close` appends a 1-turn close phase to EVERY plan (default, due-zero, due-heavy, blank, limited_time, boredom variants). Arithmetic (documented in code): the close turn is borrowed from `free` when free ≥ 2 (session stays ≈ estimate: default 14-turn plan is now 3/4/5/1/1 = 14); when free is thin (1) or absent (limited_time), close is additive +1 — never bought by zeroing free. `tutor/modes.py _phase_prefix` gained the CLOSE prefix (one English "you practiced X" line from summary data + a real Spanish farewell exchange with a farewell they have met; no new items, no corrections unless the farewell fails). `tutor/conv_session.py close_summary_block` builds the ≤2-line summary from session state only (introduced keys, error patterns resolved this session — new `ModeSessionState.resolved_this_session` accumulator —, task status, skills shown) and rides flavorable close turns with note `close_phase_offered`. PEDAGOGY §1.2 clause updated queued→shipped (version above); no other law text touched. Tests: close-last across 8 plan variants; prefix content; summary from state; guard ignores the close hint.

**2. Gate blind-spot (defect #2) — LANDED, hypothesis VERIFIED.** Log verification (`logs/sessions/20260728-103617-conversational-web.jsonl`, record 5): «¡Mucho gusto, Patrick! ¡Qué bien!» rode the **acknowledge** part; the scan's teach_blob was model+try only, so the key (in the table, `introductions` theme) was never scanned — hypothesis confirmed. Fix: `scan_unscaffolded_new_items` now scans ALL visible teaching text (the composed learner-facing reply — acknowledge/recast/explain/model/try/continue), all existing exemptions kept (learner_text skip, structural, first_seen, introduced, sheet-evidence conf, placement, flood threshold). Storm re-check: acknowledge-scanning does NOT re-open the formulaic-storm vector — the ≥3-distinct-bare flood soften covers it; the existing storm test was extended to put the greeting in acknowledge and still softens to the soft flood. Honest note: replaying the exact incident turn 5 through the widened gate yields `gate:unscaffolded_flood [mucho gusto, casa, y tú, trabajo]` — mucho gusto is now CAUGHT but that composite turn had 4 bare keys, so it rides the pre-existing STORM RESIDUAL soft path (PEDAGOGY §8 debt, unchanged by this batch); the isolated incident shape (bare acknowledge formula + otherwise-known model/try) is CRITICAL, proven by the named regression tests (`test_mucho_gusto_in_acknowledge_is_critical` / `_glossed_…_is_clean`).

**3. Correction rules (defect #3) — LANDED.** `tutor/executor.py AI_TUTOR_SYSTEM` gained a "Correction rules (always)" block: (a) NEVER confirm/praise an incorrect form (no ¡Sí!/¡Exacto!/¡Perfecto! on a recast turn) — acknowledge the MEANING, recast the FORM; (b) ONE grammatical person per repair (only the person the learner needed; no 1st+2nd person variants in one repair). Prompt text serving amended §2.5 — no gate, per the adjudication. Eval-side soft check added: `recast_no_confirm_praise` in `evals/conv_checks.py` (WARN-only — exclamation-shaped ¡Sí!/¡Exacto!/¡Perfecto! in the acknowledge of a `<recast>` turn; wording variety makes a hard fail brittle), run on every trajectory. String-presence tests in tests/test_plan_card.py.

**4. §2.1a shadow mechanism — LANDED per the closed review's (c) conditions (shadow/instruction+eval, NO gate).** Detection: `tutor/signal_classifier.py` gained `content_offer` + `self_flagged_form` labels (definitions + few-shots from the incident turns: the turn-6 weather offer and «uvia (rain)»/«hacer (I am making?)» self-flags); `OBSERVATIONAL_SIGNALS` frozenset marks them shadow-only, and the blocking classifier path in conv_session strips them before routing — select_mode routing provably unchanged (test). Instruction path (regex-visible cheap case, surface-form spotting per §4.2): `observe.detect_self_flagged_token` (gloss-guess «uvia (rain)» / quoted single token) + `conv_session.self_flag_uptake_block` appends the UPTAKE note ("give its correct pack-legal form or nearest pack paraphrase THIS turn, one model"); budget-tracked in `ModeSessionState.content_uptake_last_turn` (≥3-turn gap = §2.5-rate ≤1-per-3-turns + never-consecutive in one test); guard/repair turns excluded (they already perform uptake). Ledger: note `uptake_flagged:<token>`; eval `uptake_flag_honored` (WARN-only, measurement first) checks the flagged token's target appears in the reply. Tests: tests/test_uptake_flag.py (classifier labels, detection positive/negative incl. contractions and multiword quotes, budget block + recovery, notes, WARN evals).

**Protected behavior:** no changes to right-sized explanations, A/B scaffolded tries, or warmth paths; the guard chain, freeze semantics, and introduce machinery are untouched except for the additive close/uptake blocks.

## Incident: zero-English opening for a reset beginner (2026-07-28)

**Live complaint (verbatim):** "I reset the character sheet so I am a total beginner. The AI is using no english at all." **Log evidence** (`logs/sessions/20260728-120335-conversational-web.jsonl`, turn 1, mode=placement, reason=`blank_open_placement`): the open instructions were only "Wide-ceiling placement: short clear Spanish they can copy, but room for a stronger learner to show multi-skill Spanish. Not a Hola worksheet." — no English authorization anywhere — and the visible reply was 100% Spanish (`tl_ratio=1.00`): «¡Hola! Me llamo Marisol. Estoy muy bien hoy. ¿Y tú? ¿Cómo estás?». Turns 2–3 continued at `tl_ratio=1.00` with `gate:unscaffolded_flood` notes. This is the r6 "true-zero path is still production-biased" gap made flesh: P1 (comprehensible meaning is necessary raw material) and §2.3 (English scaffold has defined jobs) were structurally unreachable because every instruction layer (mode text, "English is a lifeline only" stance, english_wall gate) pushed Spanish-only. **Fix (instruction-level, code decides the register):** (1) `tutor/modes.py` `blank_open_placement` now requires a TRUE-ZERO OPENING — one warm English welcome sentence framing what will happen, tiny Spanish (greeting + name exchange, ≤3 items), a ≤6-word English gloss on EVERY Spanish item (zero sheet ⇒ every item is first exposure, §2.2 R-D), an explicitly bilingual try («Try: Me llamo ___ — my name is ___»), wide ceiling preserved; (2) a `ZERO_REGISTER_NOTE` overlay rides every non-open decision while the sheet stays blank AND the learner has produced no `spanish_ok` — first successful Spanish de-escalates to the standard mostly-Spanish register (graded, zero-state-only); (3) `tutor/executor.py` stance gains "EXCEPTION — true-zero learner (blank sheet): the opening turns REQUIRE English framing and a ≤6-word gloss on every Spanish item until the learner produces any Spanish" with the wall ban explicitly kept. Tests in `tests/test_modes.py` (orientation present on blank open; no leak to known opens; overlay on/off with `spanish_ok`) and `tests/test_plan_card.py`; eval `c01_placement_open` now mechanically requires English evidence in the open reply (`open_english_orientation` in `evals/conv_checks.py`). **Open dependency (output_gate.py owner):** a compliant true-zero open measures `tl_ratio` ≈ 0.32–0.40 with ≥12 alpha tokens and TRIPS `gate:english_wall` (CRITICAL → forced "Rewrite Spanish-forward" re-ask that reproduces the incident). The wall check needs the same placement exemption the r7 unscaffolded scan already has (`mode_l != "placement"`, output_gate.py:567) — proposed: `min_ratio = 0.25 if mode_l == "placement" else MIN_SPANISH_RATIO`, keeping a genuinely all-English open (ratio ≈ 0) faultable; ideally also a `blank_zero` flag threaded from conv_session for post-open zero turns carrying the overlay note.

## Incident: false planted milestones + rail grouping (2026-07-28)

**Live complaints (verbatim):** "buenos dias planted? ... it also seems to be false. Nothing I am talking to the ai about revolves around buenos dias o tardis." and word-level milestones reading as orphans ("I am supposed to be learning buenos dias?"), plus "IP-01 known ★" immediately followed by "needs re-check" on the same node. **Mechanism (defect 1):** `mark_introduced_if_visible` marked a planned key introduced whenever the tutor's reply merely CONTAINED it — coincidental/natural use ("¡Buenas tardes!" as a greeting) got recorded as curriculum, minting ledger milestones and starting retrieval ladders without an introduce move. **Fix (honesty rule):** introduction now requires evidence the INTRODUCE MOVE happened — the reply must carry the key AND the plan's scaffold: R-D → the "(≤6-word gloss)" parenthetical after the key; R-A/R-E → the plan's cognate/keyword anchor text in-reply; R-B → a teach_image with that concept actually attached this turn (belt-and-suspenders on top of the existing R-B→R-D downgrade). Detection reuses the output gate's scaffold detectors (`output_gate.gloss_after_key` / `anchor_in_reply`, made public — one definition, no drift). No evidence → the plan lapses: note `introduce_lapsed:<key>:no_scaffold`, no ledger write, no milestone, budget unconsumed; natural bare use stays with the gate's first_seen/flood machinery. **Ledger correction (append-only law kept):** new `retracted` event kind (polarity=down, `retracts`=<target kind>) in `tutor/progress_ledger.py` voids matching up events at-or-before the correction for BOTH display (`read_recent`) and dedupe (`up_keys`/`has_milestone`) — raw lines stay on disk; a crossing genuinely earned later re-mints. Executed live: retractions appended for `planted:buenos días` (session 20260728-112753) and `planted:buenas tardes` (20260728-120313); evidence check first — both keys were entirely ABSENT from the live sheet (the 12:03 learner reset wiped their introduce state) with zero use evidence, so only the milestones needed retracting; `retrieval_scheduler.retract_introduction` (allowlisted, ability fields untouched) exists for the sheet-clear case and is tested. Honest note: the archived minting turns DID carry their scaffolds in the reply text (T1 112753 "**buenos días** (good morning)"; T1 120313 "(tardy: late in day)") — the retractions stand on the flawed rule plus the reset-orphaned state: the rail was claiming curriculum for a learner whose live sheet never held it. A third retraction was appended for a `planted:buenos días` minted at 18:24 by session 20260728-122414 — that session was opened by the operator's API verification curl, not the learner (tutor opener performed to no one); its sheet shell was cleared via `retract_introduction`. **IP-01 root cause:** NOT a join bug — `can_do_known:IP-01` fired legitimately (observer note `IP-01:0.66→0.88/known`, session 112753 T2, code gate 0.80+2 solid uses held), then the 12:03 learner reset zeroed IP-01; `live_state_supports` correctly reports the live sheet no longer holds the band. The display fix suffices per the adjudication. **Display (defect 2):** `/api/progress` clusters now carry theme `groups` (association-table theme per key; skills → "Abilities", tasks → "Tasks", non-table keys → "other") — group line "Greetings — 2 planted", click-expandable (`<details>`) to item nodes with their evidence sentences; current-session groups start open so live growth stays visible. Single-state law: each event carries `display_state` ∈ {celebrated, recheck, down} (`progress_ledger.display_state`, unit-tested) and the re-check badge REPLACES the celebration icon/label ("↺" + "needs re-check", never "known ★" + badge). Assets bumped: `app.js?v=20260728h`, `styles.css?v=20260728d`. **Validation:** `pytest tests/ -q` 486 passed + 17 subtests; new tests in `tests/test_introduce_router.py::TestMarkOnVisible` (gloss marks / buenas-tardes natural-greeting lapse verbatim / keyword+cognate anchor evidence / R-B image attach / downgraded-plan gloss demand) and `tests/test_progress_ledger.py::TestRetraction` + `TestGroupingAndDisplayState`; truncation gate ok (14 files); server restarted, `/api/health` version 20260728-122224 `stale_code: false`; live `/api/progress` shows retracted pairs absent, `hola` planted under Greetings (its 18:10 introduce move carried the "hello" anchor — genuine, kept), IP-01 under Abilities in the single `recheck` state.

## Closing batch landed (agent, 2026-07-28)

Four items from the closing batch (debug box; english_wall zero exemption; repetition fixes; session-reset race). `pytest tests/ -q`: **526 passed, 17 subtests passed** (0 failures). `scripts/check_teacher_truncation.py`: ok (14 files). Server restarted; `/api/health` version `20260728-124829`, `stale_code: false`. Assets bumped: `app.js?v=20260728i`, `styles.css?v=20260728e` (§4.4).

**1. Debug log box (user request) — LANDED.** Server: `tutor/conv_session.py` gained `build_debug_entry` + `ConversationalSession._capture_debug_request` — per tutor call the OUTBOUND request is captured: ordered system blocks (marker-derived label + FULL text + cache flag), chat history (role + full content), the task message, model name, plus response metadata (usage incl. thinking/cached tokens, gate faults/notes, turn notes, mode/reason/activity, stop_reason). Ring buffer `deque(maxlen=10)` per session, IN-MEMORY ONLY — nothing added to disk logs (session .jsonl unchanged; the `[:200]` in the label sniffer carries a `truncation-ok` marker, full text stored). `GET /api/debug/requests` (web_app.py) serves the current session newest-first; no session → valid empty JSON. Capture is on the default AI-tutor path (`_execute_ai_tutor`); the optional TEACHER_MODE=rules/legacy paths are not captured (deviation, product path only). Client: collapsible "Debug — AI tutor requests" box at the page bottom (default collapsed; open state in `localStorage.debugOpen`); per-turn entry summary `turn n · mode/reason · activity · in/out/think tokens` expanding to SYSTEM BLOCKS (each labeled + collapsible), INSTRUCTIONS (the mode-decision instruction stack as its own always-open section), TASK MESSAGE, HISTORY (count + expandable), RESPONSE META; monospace, scrollable, per-entry Copy-as-JSON button; refreshed after every chat turn / open / reset while open. Live verification: one `/api/chat` turn against the restarted server → 2 entries, labels `tutor_stance/persona/course_pack`, instructions present (test cost: open+chat ≈ $0.065 session total). Tests: `tests/test_debug_requests.py` (ring caps at 10; entry shape incl. system blocks + instructions; endpoint newest-first + empty-JSON case).

**2. english_wall zero exemption — LANDED (as specified by the prior agent's measurements).** `output_gate.py`: `ZERO_MIN_SPANISH_RATIO = 0.25`; the wall check uses `min_ratio = 0.25 if (mode=='placement' or blank_zero) else MIN_SPANISH_RATIO`. `blank_zero` is threaded from conv_session with exactly the ZERO_REGISTER_NOTE overlay condition (`blank and 'spanish_ok' not in sigs`) so the zero-register turns riding conversation/repair modes get the same floor the placement open does. Genuine all-English (ratio ≈ 0) still faults in every mode. Tests (`TestEnglishWallZeroExemption`): compliant zero opening fixture measured ratio 0.35 / alpha 34 passes in placement AND in a blank_zero conversation turn; same turn without the flag still walls (normal thresholds unchanged); all-English faults in both exempt states; all pre-existing english_wall tests green.

**3. Repetition fixes (forensics' three classes) — LANDED.**
(a) *Asked-topic registry.* `SessionMemory` gained `asked_topics` + `note_asked_topic(frame, concept)` storing semantic keys ("size:ciudad", "location:casa") and snapshot exposure; extractor `topic_key_for_try` in session_memory.py (frames: `cómo estás→wellbeing`, `cómo te llamas→name`, `dónde/where→location`, `grande|pequeñ|size/big/small→size`, `qué+verb→what:<verb>`; concept = first pack/table noun in the try via `word_present`, accent-normalized; noun palette = code-owned list with the incident nouns ciudad/casa leading + association-table keys). conv_session notes the key post-turn (`asked_topic:<key>` note). Executor payload: `topics_tutor_already_asked` (mode names — garbage) REPLACED by `do_not_re_ask` from the registry.
(b) *Guard-6 escape hatch closed.* `modes._noun_from_text`: the `conf < 0.25` re-fire branch DELETED — images_shown/covered membership is authoritative. conv_session records the guard-6 concept as covered (`SessionMemory.covered_concepts`, no image/lexicon write required) and passes `images_shown | covered_concepts` into select_mode, so `new_noun:<c>` cannot re-fire this session at all.
(c) *gate:probe_loop generalized.* The 4 social regexes stay as fast-path; `check_output_gate` now also derives the composed try's semantic key (same extractor) and faults `gate:probe_loop` when it is already in the session's `asked_topics` (existing repair-instruction machinery reused; wording generalized). Tests: verbatim incident try (session 20260728-120335 turn 3, «¿Y tu casa? ¿Está en una ciudad grande o en una ciudad pequeña?» → `size:ciudad`) faults on the second identical ask; a NEW frame on the same concept («¿Qué hay en tu casa?» → `what:hay:casa` after `location:casa`) does NOT fault; social fast-path regression green; guard-6 shown-concept-at-conf-0.0 no-refire + first-mention control in `tests/test_modes.py`; registry/extractor/payload in `tests/test_asked_topics.py`.

**4. Session-reset race — LANDED.** Server (`web_app.py`): `/api/session/reset` now FULLY replaces the session — the cookie's session closes with the reset-sheet semantics AND every other live session is closed (session_end written) before the one new session is created and its sid cookie returned; `_get_or_create` is tolerant — a DEAD cookie sid closes all unreachable orphans before creating the replacement (a request with NO cookie deliberately does not, so operator curls can't kill a live learner session — the reaper owns that class); orphan reaper: sessions with no activity for 2h (`IDLE_REAP_SEC`) are `close()`d and dropped by `_purge_stale` (previously 8h TTL delete). Client (`app.js`): session-lifecycle single-flight lock (`sessionFlowInFlight`) around startSession / New chat / Reset learner — every reset AWAITS its response (and Set-Cookie) under the lock before any other session-creating call can begin; the reset response IS the new session's opening turn (no separate startSession fires). Tests: `tests/test_web_sessions.py` — reset→start sequence yields ONE live session (dead-cookie incident shape, orphan closed, Set-Cookie asserted); reset closes cookie session + orphans with sheet persisted; 2h reaper closes-and-drops idle sessions, young sessions survive; dead-cookie `_get_or_create` reaps; no-cookie create leaves others alone. All via a monkeypatched fake session — no LLM calls.

**Protected behavior:** guard chain, freeze semantics, introduce machinery, gate criticality set (probe_loop stays soft — repair rides co-occurring critical faults, per current policy), and session .jsonl logging untouched. Nothing committed (per instruction).

## Incident: journey rail display + operator pollution (2026-07-28)

**Live complaint (verbatim, pre-session):** the Journey card read "2026-07-28 / Greetings — 1 planted / 2026-07-28 / Greetings — 2 planted / 2026-07-28 / Greetings — 1 planted / 2026-07-28 / Abilities — 1 known" — "That is not helpful or pretty or anything." Two defects: (1) per-SESSION clusters made one afternoon read as four identical date-stamped journeys, with collapsed "Theme — N planted" rows hiding the actual items behind counts; (2) three of the four clusters were operator pollution — agent verification chats, not the learner. **Display fix:** `/api/progress` now clusters by LOCAL calendar day (`progress_ledger.read_recent_days` — sessions within a day merged, "· N sessions" suffix when >1, `session_id` kept per event for tooltip/debug) and humanizes keys server-side (`humanize_event`: lexicon → the Spanish with association-table gloss on hover; can-do ids → statement snippet via `can_do_display`, mastery frame "Can …" ONLY at the known band per the amended design; task keys → scene goal via `default_scene_goals`; unknown keys → raw key fallback, nothing invented). Client (`app.js`/`styles.css`) renders a glanceable path: vertical spine with a dot per day, day headers as small date chips ("Today"/"Yesterday"/"Jul 26"), milestones as inline icon+name chips (🌱 hola · ★ Can greet a peer), theme surviving only as a tiny muted prefix/ordering, current day accented, single-state law kept (re-check REPLACES celebration), retracted pairs never rendered. Assets bumped: `app.js?v=20260728j`, `styles.css?v=20260728f`. **Operator pollution retracted (append-only; raw lines preserved):** `can_do_known:IP-01` (session 20260728-112753 — known operator session, 2 turns, single generic "Hola, estoy bien hoy."), `planted:buenos días` + `planted:buenas tardes` (20260728-124909 — one generic "good morning" turn bracketed by controller-demo-offline automation 12:47–12:53, force-closed by the 13:09 sweep), `planted:buenas noches` (20260728-125628 — ZERO learner turns; session_end 16 ms after the auto-opener, inside the 12:56–12:57 automation burst). Genuine learner sessions 20260728-103617 and 20260728-120335 untouched — the rail now shows exactly one honest cluster: Today · 🌱 hola. Sheet-side: live sheet lexicon is EMPTY and IP-01 is unknown/0.0 with no evidence (the 12:03 reset), so `retract_introduction` had nothing to clear — no ambiguous cases left standing. **Guard:** `PROGRESS_LEDGER_PATH` env override confirmed in `default_ledger_path` (eval harness `evals/run_conv_smoke.py` already isolates per-trajectory); module docstring now states that verification chats against the live server MUST set it. **Validation:** `pytest tests/ -q` 553 passed + 17 subtests (new: `TestDayClustering` incl. cross-midnight split, `TestHumanization`, `TestOperatorRetractionDisplay` — raw-present/display-absent); truncation gate ok (14 files); payload verified OFFLINE against the live ledger (no live chat — no new pollution). Server left STALE deliberately (`/api/health` running 20260728-130830 vs disk 20260728-132202) — the parallel agent owns the restart; static assets activate on browser reload, `/api/progress` day-clustering activates when that restart lands (new client falls back cleanly against the old payload until then).
